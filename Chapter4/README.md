# Chapter4 - Imperative (looking) Haskell

* `IO`-functions

* The `main` function and do notation

* Writing `IO` functions

* The `return` function

* Passing `IO` functions around

* `let` in `do`

* The `<$>` operator
